Snake game in Python! To run, do the following:

```
$ python3 -m venv .venv # Create a virtual environment
$ source .venv/bin/activate # Activate it
$ pip install -r requirements.txt # Install dependencies
$ python3 cobrinha.py
```
